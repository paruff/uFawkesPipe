# uFawkesPipe — CI/CD Plane of the Fawkes IDP
# Stack nickname: ufawkespipe (used in service labels)
# User-facing product name: uFawkesPipe
# License: Apache 2.0

name: ufawkespipe

services:

  woodpecker-server:
    image: woodpeckerci/woodpecker-server:v3.15.0
    ports:
      - "8000:8000"    # UI
      - "9000:9000"    # gRPC agent communication
    environment:
      - WOODPECKER_OPEN=true
      - WOODPECKER_HOST=${WOODPECKER_HOST:-http://localhost:8000}
      - WOODPECKER_GITHUB=true
      - WOODPECKER_GITHUB_CLIENT=${WOODPECKER_GITHUB_CLIENT}
      - WOODPECKER_GITHUB_SECRET=${WOODPECKER_GITHUB_SECRET}
      - WOODPECKER_AGENT_SECRET=${WOODPECKER_AGENT_SECRET}
      - WOODPECKER_DATABASE_DRIVER=sqlite3
      - WOODPECKER_DATABASE_DATASOURCE=/var/lib/woodpecker/woodpecker.sqlite
    volumes:
      - woodpecker_data:/var/lib/woodpecker
    labels:
      - "plane=ufawkespipe"
      - "component=ci-server"
      - "managed-by=fawkes"
    restart: unless-stopped

  woodpecker-agent:
    image: woodpeckerci/woodpecker-agent:v3.15.0
    environment:
      - WOODPECKER_SERVER=woodpecker-server:9000
      - WOODPECKER_AGENT_SECRET=${WOODPECKER_AGENT_SECRET}
      - WOODPECKER_BACKEND=docker
      - WOODPECKER_BACKEND_DOCKER_NETWORK=ufawkespipe_default
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    depends_on:
      - woodpecker-server
    labels:
      - "plane=ufawkespipe"
      - "component=ci-agent"
      - "managed-by=fawkes"
    restart: unless-stopped

  sonarqube:
    image: sonarqube:lts-community
    ports:
      - "9001:9000"
    environment:
      - SONAR_ES_BOOTSTRAP_CHECKS_DISABLE=true
    volumes:
      - sonarqube_data:/opt/sonarqube/data
      - sonarqube_logs:/opt/sonarqube/logs
      - sonarqube_extensions:/opt/sonarqube/extensions
    labels:
      - "plane=ufawkespipe"
      - "component=sast"
      - "managed-by=fawkes"
    restart: unless-stopped

  portainer:
    image: portainer/portainer-ce:2.39.3
    ports:
      - "9443:9443"    # HTTPS UI (default from CE 2.9+)
      - "9002:8000"    # Edge agent tunnel
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - portainer_data:/data
    labels:
      - "plane=ufawkespipe"
      - "component=cd-engine"
      - "managed-by=fawkes"
    restart: unless-stopped

volumes:
  woodpecker_data:
  sonarqube_data:
  sonarqube_logs:
  sonarqube_extensions:
  portainer_data:
