{
  "feature": "PIPE-004",
  "title": "Fix Stale File Reference in workflow-agent.md",
  "description": "Update docker-compose.yml reference to compose.yaml in the context files table of .agents/agents/workflow-agent.md.",
  "branch": "feature/pipe-004-fix-workflow-agent-stale-ref",
  "tasks": [
    {
      "id": "PIPE-004-01",
      "title": "Update context files table reference",
      "description": "Change docker-compose.yml to compose.yaml in the context files table of .agents/agents/workflow-agent.md.",
      "depends_on": [],
      "acceptance": [
        "context files table references compose.yaml",
        "no docker-compose.yml references remain in the file"
      ]
    },
    {
      "id": "PIPE-004-02",
      "title": "Verify no other stale references",
      "description": "Read full file to confirm no other stale references exist.",
      "depends_on": ["PIPE-004-01"],
      "acceptance": [
        "grep docker-compose.yml .agents/agents/workflow-agent.md returns empty",
        "markdownlint passes on the file"
      ]
    }
  ]
}
